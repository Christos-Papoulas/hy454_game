#include "header_files\collision\FullCollision.h"


void FullCollision::UpdateMap() {
	Dim x,y;
	x = Terrain::GetTileLayer()->GetViewWindow().GetX();
	y = Terrain::GetTileLayer()->GetViewWindow().GetY();
	
}